import { TitleMagentaDirective } from './title-magenta.directive';

describe('TitleMagentaDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleMagentaDirective();
    expect(directive).toBeTruthy();
  });
});
